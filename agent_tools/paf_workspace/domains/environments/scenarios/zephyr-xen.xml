<paf_config>
    <phase name="ensure">
        <task name="paf_workspace.domains.environments.tasks.zephyr_xen.ensure_environment_image"/>
    </phase>

    <phase name="image_check">
        <task name="paf_workspace.domains.environments.tasks.zephyr_xen.check_environment_image"/>
    </phase>

    <phase name="check">
        <task name="paf_workspace.domains.environments.tasks.zephyr_xen.check_zephyr_xen_tools"/>
    </phase>

    <phase name="validate">
        <task name="paf_workspace.domains.zephyr_repo_validation.tasks.build.validate_zephyr_build"/>
    </phase>

    <phase name="push_guard">
        <task name="paf_workspace.tasks.record_push_guard_success"/>
    </phase>

    <phase name="run_container_command">
        <task name="paf_workspace.domains.environments.tasks.base.run_container_command"/>
    </phase>

    <scenario name="check-only">
        <phase name="image_check"/>
        <phase name="check"/>
    </scenario>

    <scenario name="validate">
        <phase name="ensure"/>
        <phase name="validate"/>
        <phase name="push_guard"/>
    </scenario>

    <scenario name="run-container-command">
        <phase name="ensure"/>
        <phase name="run_container_command"/>
    </scenario>
</paf_config>
