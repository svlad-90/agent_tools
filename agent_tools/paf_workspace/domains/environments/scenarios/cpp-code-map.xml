<paf_config>
    <phase name="ensure">
        <task name="paf_workspace.domains.environments.tasks.base.ensure_environment_image"/>
    </phase>

    <phase name="image_check">
        <task name="paf_workspace.domains.environments.tasks.base.check_environment_image"/>
    </phase>

    <phase name="check">
        <task name="paf_workspace.domains.environments.tasks.base.check_workspace_tool_baseline"/>
        <task name="paf_workspace.domains.environments.tasks.base.check_cpp_code_map_tools"/>
    </phase>

    <phase name="run_container_command">
        <task name="paf_workspace.domains.environments.tasks.base.run_container_command"/>
    </phase>

    <scenario name="check-only">
        <phase name="image_check"/>
        <phase name="check"/>
    </scenario>

    <scenario name="validate">
        <phase name="ensure"/>
        <phase name="check"/>
    </scenario>

    <scenario name="run-container-command">
        <phase name="ensure"/>
        <phase name="run_container_command"/>
    </scenario>
</paf_config>
