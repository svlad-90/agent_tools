# Task Actions Component

Owns `TASK_ACTIONS.json` loading and the action model visible to services and
UIs.

Use only `agent_tools.tools.agent_workspace.components.task_actions.api` from
outside this component.

