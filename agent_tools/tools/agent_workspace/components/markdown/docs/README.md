# Markdown Component

Renders the small markdown subset used by Agent Workspace details panes and
provides the rough token counter used for workspace/task context estimates.

Public callers use `agent_tools.tools.agent_workspace.components.markdown.api`.
Implementation details stay under `src`.
