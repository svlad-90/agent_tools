# Task Sessions Component

Owns persisted AI-agent session state and background session discovery.

Use only `agent_tools.tools.agent_workspace.components.task_sessions.api` from
outside this component.

